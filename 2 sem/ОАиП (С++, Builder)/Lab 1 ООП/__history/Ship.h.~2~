//---------------------------------------------------------------------------

#ifndef ShipH
#define ShipH

#include "Rect.h"

class  Ship: public Rect
{
    int x1,y1,x2, y2;
	public:
	Ship(){}  //конструктор по умолчанию
	Ship(int, int, int, int);//конструктор
	~Ship(){}   //деструктор
	void show(TCanvas *Canvas);
};

#endif
